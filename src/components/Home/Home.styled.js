import styled from 'styled-components';

export const PopularMoviesList = styled.ul`
  margin: ${p => p.theme.spacing(3)};
`;

export const PopularMoviesItem = styled.li`
  font-size: 20px;
  margin: ${p => p.theme.spacing(1)};
`;
